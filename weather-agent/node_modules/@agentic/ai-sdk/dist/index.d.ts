import * as ai from 'ai';
import * as zod from 'zod';
import { AIFunctionLike } from '@agentic/core';

/**
 * Converts a set of Agentic stdlib AI functions to an object compatible with
 * the Vercel AI SDK's `tools` parameter.
 */
declare function createAISDKTools(...aiFunctionLikeTools: AIFunctionLike[]): {
    [k: string]: ai.Tool<zod.ZodObject<any, zod.UnknownKeysParam, zod.ZodTypeAny, {
        [x: string]: any;
    }, {
        [x: string]: any;
    }> | ai.Schema<unknown>, unknown> & {
        execute: (args: unknown, options: ai.ToolExecutionOptions) => PromiseLike<unknown>;
    };
};

export { createAISDKTools };
